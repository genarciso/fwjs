import main from "../../components/Main/main.svelte";

export default {
    title: 'Tab/Main',
    component: main,
};

const Template = () => ({
  Component: main
});

export const Main = Template.bind({});
